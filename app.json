{
  "name": "Visualizzatore log Modmail (per VincyBot07).",
  "description": "Un webserver per vedere i log hostati sul MongoDB.",
  "repository": "https://github.com/VincyBot07/logviewer",
  "env": {
    "MONGO_URI": {
        "description": "L'URI del tuo MongoDB che conterrà i log.",
        "required": true
        }
    }
}
